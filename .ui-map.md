# UI Map

## ID Rules

- Prefix: `U`
- Format: `Uxxx`
- Next ID: `U011`
- Reuse existing IDs.
- Do not duplicate IDs.

## Status values: `open` | `wip` | `partial` | `done` | `blocked`
## Risk values: `low` | `medium` | `high`

## Entries

| ID | Status | Risk | Area | Files | Depends on | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| U001 | open | medium | Live camera UI freeze risk | `BeeInterface/Group/View.cs` | — | `BeeCore.Common.listCamera[Global.IndexCCCD].Read()` called directly in UI-facing `Live()` and `StopLive()` paths. If camera read blocks on SDK IO, WinForms UI freezes during live toggle or stop. Fix: offload to background thread or use `async/await` with `BeginInvoke` for UI update. |
| U002 | open | medium | Capture/read blocking path | `BeeInterface/Group/View.cs` | — | `Global_StatusProcessingChanged` performs `camera.Read()` inside the `StatusProcessing.Read` branch for every camera, including a second read for USB cameras. Capture latency can block status transitions and make UI appear stuck during trigger/read. |
| U003 | open | medium | Cross-thread UI updates | `BeeInterface/Group/View.cs` | — | Mixes direct UI writes and `Invoke`/`BeginInvoke` in status/camera callbacks: direct `G.StatusDashboard.StatusText` writes in processing-state handling, direct camera label updates in `Global_CameraStatusChanged` for `NotConnect`/`Ready`, while other branches marshal to UI thread. All UI writes from non-UI threads must use `Invoke`/`BeginInvoke`. |
| U004 | open | low | Live frame preview cost | `BeeInterface/Group/View.cs` | — | Converts camera frames to `Bitmap` for preview in `PylonCam_FrameReady` and many `matRaw.ToBitmap()` paths. Can slow rendering and increase GDI churn during live preview when display FPS is lower than camera FPS. Related to O001/O004. |
| U005 | open | low | Dropped frame visibility | `BeeInterface/Group/View.cs` | — | Live display loop keeps only one shared frame and drops frames when `_uiPending` is already set, but UI does not expose dropped-frame count or display FPS. Operators see lag/stutter without knowing whether acquisition or rendering is the bottleneck. |
| U006 | open | medium | Start/stop button state safety | `BeeInterface/Group/View.cs` | — | `btnCap_Click`, `btnRecord_Click`, `Live()`, and timer paths manually toggle `btnCap`, `btnContinuous`, `btnLive`, `Global.StatusMode`, and timers in separate branches. Missing central state ownership makes double-clicks or rapid mode changes risky during camera operations. |
| U007 | open | medium | Blocking error dialogs | `BeeInterface/Group/View.cs` | — | `Global_CameraStatusChanged` and `Global_PLCStatusChanged` show modal `ForrmAlarm.ShowDialog()` from status-change handling. Blocks UI thread during camera/PLC faults and can interrupt recovery or stop/start actions. |
| U008 | partial | low | Tool run disable/enable gaps | `BeeInterface/Tool/ToolColorArea.cs`, `BeeInterface/Tool/ToolCircle.cs`, other `Tool*.cs` | R003 | Resolution so far: `ToolColorArea_StatusToolChanged` now marshals back to UI thread before re-enabling `btnInspect`/`btnCalib` on `StatusTool.Done`. Remaining: same marshal pattern needed in `ToolCircle` and other `Tool*.cs` test/calibration buttons. Blocked on R003 (ToolCircle compile break). |
| U009 | open | low | Overloaded forms | `BeeInterface/Tool/ToolYolo.cs`, `BeeInterface/Tool/ToolPattern.cs`, `BeeInterface/GeneralSetting.cs`, `BeeInterface/Tool/ToolCounter.cs`, `BeeInterface/PLC/ProtocolPLC.cs`, `BeeInterface/Group/View.cs` | — | Handler counts: ToolYolo≈135, ToolPattern≈86, GeneralSetting≈85, ToolCounter≈83, ProtocolPLC≈82, View≈71. Increases UI regression risk because camera, processing, status, and control logic are concentrated in form classes. |
| U010 | open | low | Error/status clarity | `BeeInterface/Group/View.cs`, `BeeInterface/Tool/*.cs` | — | Many UI catches near camera/tool operations catch `Exception` but status display is inconsistent across `StatusDashboard`, labels, dialogs, logs, and silent catches. Operators may not get a clear recoverable error state when camera grab or processing fails. |
