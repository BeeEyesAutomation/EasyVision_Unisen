# Optimization Map

## ID Rules

- Prefix: `O`
- Format: `Oxxx`
- Next ID: `O009`
- Reuse existing IDs.
- Do not duplicate IDs.

## Status values: `open` | `wip` | `partial` | `done` | `blocked`
## Risk values: `low` | `medium` | `high`

## Entries

| ID | Status | Risk | Area | Files | Depends on | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| O001 | open | low | Live frame bitmap conversion | `BeeInterface/Group/View.cs` | — | Converts camera frames to `Bitmap` in Pylon callback (`PylonCam_FrameReady`, `BitmapConverter.ToBitmap`) and has many additional `matRaw.ToBitmap()` paths. Baseline: `Select-String -Path BeeInterface\Group\View.cs -Pattern 'ToBitmap\(|BitmapConverter\.ToBitmap'` and manually measure live-view CPU/GDI object count while camera streams. |
| O002 | open | low | Camera frame drop/display queue | `BeeInterface/Group/View.cs` | — | Keeps only newest frame with `Interlocked.Exchange(ref _sharedFrame, bmp)` and `DisplayLoop` waits up to 50 ms on `_frameReady`; intentionally drops frames but caps display near 20 FPS even if acquisition is faster. Baseline: compare `BeeCore.Common.listCamera[Global.IndexCCCD].FrameRate` / Pylon measured FPS against displayed FPS during live mode. |
| O003 | open | medium | Synchronous camera read bridge | `BeeCore/Func/Camera.cs`, `BeeInterface/Group/View.cs` | — | `Camera.Read()` calls `ReadAwait().GetAwaiter().GetResult()` and `View.cs` calls `Read()` from UI/workflow paths. Baseline: time `BeeCore.Common.listCamera[Global.IndexCCCD].Read()` with `Stopwatch` around manual snap/live transitions and record UI responsiveness. |
| O004 | open | low | Repeated image copy counts | `BeeInterface/Group/View.cs`, `BeeCore/Func/Camera.cs`, `BeeCore/Unit/Patterns.cs`, `BeeCore/Unit/Yolo.cs` | — | Baseline scan: `View.cs` ToBitmap=32, Clone=8, new Mat=13; `Camera.cs` Clone=11, new Mat=18; `Patterns.cs` Clone=18, new Mat=35; `Yolo.cs` new Mat=24. Baseline command: `Select-String` counts for `ToBitmap\(|\.Clone\(|new Mat\(` on these files before each optimization pass. |
| O005 | open | medium | Managed/unmanaged sample buffer copy | `BeeCore/Unit/MultiPattern.cs` | — | `Pattern2.SetImgeSample` allocates unmanaged memory and copies native `Mat` data back to C#; callers wrap pointer in `Mat` and clone to bitmap. Baseline: time `SetImgeSample` through bitmap creation with `Stopwatch` and track process private bytes during repeated calibration. |
| O006 | partial | low | Pixel loops in filters | `BeeCore/Algorithm/Filters.cs` | — | `SuppressHighlight` now uses row pointers instead of `At<byte>`/`Set(...)` inside the nested pixel loop. Before: 2 `At<byte>` reads + 1 `Set(...)` write per pixel. After: 0 `At<byte>`, 0 `Set(...)`, 3 `Ptr(...)` row lookups per row. Remaining: verify image correctness with representative before/after output diff when vision test harness is available. |
| O007 | open | low | File/image load display path | `BeeInterface/Group/View.cs` | — | File navigation calls `Cv2.ImRead`, clone/add to `listMat`, call `Native.SetImg(matRaw.Clone())`, and assign `imgView.Image = matRaw.ToBitmap()`. Baseline: measure next/previous image latency with `Stopwatch` around lines that load from `Files[indexFile]` and update the view. |
| O008 | open | low | Tool processing allocation churn | `BeeCore/Unit/ColorArea.cs`, `BeeCore/Unit/Width.cs`, `BeeCore/Unit/Patterns.cs`, `BeeCore/Unit/Yolo.cs`, `BeeCore/Unit/MultiPattern.cs` | — | Vision tools repeatedly dispose/recreate `Mat` instances during inspection. Baseline: run one inspection cycle with representative tools enabled and log elapsed ms plus Gen0/Gen1 collections via `GC.CollectionCount(...)`. |
