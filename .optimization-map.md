# Optimization Map

## ID Rules

- Prefix: `O`
- Format: `Oxxx`
- Next ID: `O009`
- Reuse existing IDs.
- Do not duplicate IDs.

## Status values: `open` | `wip` | `partial` | `done` | `blocked`
## Risk values: `low` | `medium` | `high`

## Entries

| ID | Status | Risk | Area | Files | Depends on | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| O001 | done | low | Live frame bitmap conversion | `BeeInterface/Group/View.cs` | — | Pylon callback now copies live frames into the existing live bitmap via `UpdateGray`/`UpdateBgr` instead of allocating a new `Bitmap` with `BitmapConverter.ToBitmap` per frame. Build verified Release x64. |
| O002 | done | low | Camera frame drop/display queue | `BeeInterface/Group/View.cs` | — | `UpdateBgr` now allows only one pending WinForms invalidate while still copying newer frames into the reusable live bitmap, preventing queued redraw backlog during fast live acquisition. Build verified Release x64. |
| O003 | partial | medium | Synchronous camera read bridge | `BeeCore/Func/Camera.cs`, `BeeInterface/Group/View.cs` | — | `View.Live()` now offloads its initial `camera.Read()` to `Task.Run` before updating live preview state, reducing UI-thread blocking during live start. Remaining synchronous read bridges in capture/status paths should be handled under the existing UI/capture entries. Build verified Release x64. |
| O004 | partial | low | Repeated image copy counts | `BeeInterface/Group/View.cs`, `BeeCore/Func/Camera.cs`, `BeeCore/Unit/Patterns.cs`, `BeeCore/Unit/Yolo.cs` | — | `View.DrawImage` now reuses the current displayed image when available and disposes the fallback `matRaw.ToBitmap()` immediately. Remaining copy hotspots across camera/tool paths still need separate passes. Build verified Release x64. |
| O005 | open | medium | Managed/unmanaged sample buffer copy | `BeeCore/Unit/MultiPattern.cs` | — | `Pattern2.SetImgeSample` allocates unmanaged memory and copies native `Mat` data back to C#; callers wrap pointer in `Mat` and clone to bitmap. Baseline: time `SetImgeSample` through bitmap creation with `Stopwatch` and track process private bytes during repeated calibration. |
| O006 | partial | low | Pixel loops in filters | `BeeCore/Algorithm/Filters.cs` | — | `SuppressHighlight` now uses row pointers instead of `At<byte>`/`Set(...)` inside the nested pixel loop. Before: 2 `At<byte>` reads + 1 `Set(...)` write per pixel. After: 0 `At<byte>`, 0 `Set(...)`, 3 `Ptr(...)` row lookups per row. Remaining: verify image correctness with representative before/after output diff when vision test harness is available. |
| O007 | partial | low | File/image load display path | `BeeInterface/Group/View.cs` | — | File display paths now use `ShowCurrentRawImage(...)` so previous `imgView.Image` bitmaps are disposed consistently and native handoff/display conversion are centralized. Remaining: folder load still eagerly materializes `listMat` and should be measured before a lazy-load change. Build verified Release x64. |
| O008 | partial | low | Tool processing allocation churn | `BeeCore/Unit/ColorArea.cs`, `BeeCore/Unit/Width.cs`, `BeeCore/Unit/Patterns.cs`, `BeeCore/Unit/Yolo.cs`, `BeeCore/Unit/MultiPattern.cs` | — | `ColorArea` now transfers the gray conversion result directly into `matProcess` instead of cloning then disposing the temporary. Remaining allocation churn in Width/Patterns/Yolo/MultiPattern needs separate measured passes. Build verified Release x64. |
