# Code Review Map

## ID Rules

- Prefix: `R`
- Format: `Rxxx`
- Next ID: `R009`
- Reuse existing IDs.
- Do not duplicate IDs.

## Status values: `open` | `wip` | `partial` | `done` | `blocked`
## Risk values: `low` | `medium` | `high`

## Entries

| ID | Status | Risk | Area | Files | Depends on | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| R001 | blocked | medium | Broken references | `BeeCore/BeeCore.csproj`, `BeeGlobal/BeeGlobal.csproj`, `BeeInterface/BeeInterface.csproj`, `BeeUi/BeeUi.csproj` | — | Several `HintPath` values point to ignored build-output binaries (`../bin/Release/LibUsbDotNet.dll`, `../BeeCore/bin/x64/Release/OpenCvSharp*.dll`). Files exist locally but are untracked, so a clean checkout loses references. Fix: replace HintPath with NuGet package references or a committed vendor path. Blocked: HintPath fix requires committing vendor DLLs or finding a compatible OpenCvSharp NuGet version. Deferred pending version investigation. |
| R002 | blocked | high | Solution build verification | `EasyVision.sln`, `BeeUpdate.csproj`, all `.vcxproj` | — | `dotnet build EasyVision.sln --no-restore` does not complete: native `.vcxproj` files require `$(VCTargetsPath)` (Visual Studio C++ targets) and `BeeUpdate.csproj` hits an x86 `GenerateResource` task-host issue. Verification blocker until MSBuild/VS C++ toolchain is available. Use `MSBuild` via VS Developer Command Prompt instead of `dotnet build`. |
| R003 | done | low | WinForms designer handlers | `BeeInterface/Tool/ToolCircle.cs`, `BeeInterface/Tool/ToolCircle.Designer.cs` | — | Verified: all `ToolCircle.Designer.cs` event-handler targets are present across the `ToolCircle` partial class files, and Release x64 build passes. No Designer.cs edit was needed. |
| R008 | done | low | ToolPattern copy opens disposed template image | `BeeCore/Unit/Patterns.cs` | — | Fixed copy/reset path so `ResetRuntimeStateForCopy()` no longer disposes `bmRaw`. Copy flow can assign `bmRaw` to `ToolPattern.imgTemp.Image` before `SetModel(true)` runs; disposing it leaves PictureBox with an invalid Bitmap and can throw `Parameter is not valid` when the copied tool is opened. Build verified Release x64 with 424 warnings. |
| R004 | open | low | Dead code — Gap.DrawInfiniteLine | `BeeCore/Algorithm/Gap.cs` | — | `DrawInfiniteLine` (~line 735) is defined but all call sites are commented out (lines 259, 311-312). Safe to delete: remove method body and any commented-out call lines. Build verify after. |
| R005 | done | low | Dead code — FilletCornerMeasure NormalizeDeg / AngleDiff180 | `BeeCore/Algorithm/FilletCornerMeasure.cs`, `BeeCore/Algorithm/FilletCornerMeasure2.cs` | S011 | Audit 2026-05-11: methods are now active. `NormalizeDeg` and `AngleDiff180` are called inside the same files (FilletCornerMeasure.cs:138/139/143/144, FilletCornerMeasure2.cs:244/245/249/250). Closed as not-dead. If S011 later extracts these to `ContourGeometry`, re-open with new ID. |
| R006 | done | low | Dead code — Patterns.cs cluster (8 methods) | `BeeCore/Unit/Patterns.cs` | — | Audit 2026-05-11: all 8 methods now have callers in the same file — `ResetRuntimeStateForCopy` (Patterns.cs:1563), `ClearResultItems` (1635), `HasMaskColor` (775), `HasNgColor` (1364), `LearnColorMaskContours` (513), `AddColorCore` (632, 643), `ToAbsoluteRectRotate` (898), `AddMatchResult` (1768, 1788). Closed as not-dead. |
| R007 | done | low | Dead code — FilletCornerMeasure2.IsLocallyStraight | `BeeCore/Algorithm/FilletCornerMeasure2.cs` | — | Audit 2026-05-11: `IsLocallyStraight` (FilletCornerMeasure2.cs:135) is now called at lines 264, 840, 877. Closed as not-dead. |
