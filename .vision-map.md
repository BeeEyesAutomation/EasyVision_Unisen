# Vision Map

## ID Rules

- Prefix: `V`
- Format: `Vxxx`
- Next ID: `V010`
- Reuse existing IDs.
- Do not duplicate IDs.

## Status values: `open` | `wip` | `partial` | `done` | `blocked`
## Risk values: `low` | `medium` | `high`

## Entries

| ID | Status | Risk | Area | Files | Depends on | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| V001 | done | medium | YOLO result filtering behavior | `BeeCore/Unit/Yolo.cs` | — | Restored multi-detection result handling after merge/remove filtering: filtered detections are all added in score order instead of only `FirstOrDefault`, and scan boxes produce NG when detections exist but all are rejected by the crop-scan filter. |
| V002 | done | medium | Measurement scoring behavior | `BeeCore/Unit/ColorArea.cs`, `BeeCore/Unit/Width.cs` | V004 | Restored Width score result to percent deviation from `WidthTemp` with a zero guard, and guarded ColorArea ROI offset so invalid offsets cannot collapse the measured rectangle. |
| V003 | done | medium | Filter/noise behavior | `BeeCore/Algorithm/Filters.cs` | — | `ClearNoise` now normalizes input to a single-channel binary mask before connected-components cleanup, keeps `Scalar.All(...)` for label matching, and returns a stable `CV_8UC1` cleanup mask for downstream vision tools. |
| V004 | done | low | Width edge mode regression | `BeeCore/Unit/Width.cs` | — | Restored the missing `MethordEdge.InvertBinary` branch in the Width edge-processing switch so saved tools assign `matProcess` with an inverse binary threshold before noise/morphology and measurement steps. |
| V005 | done | medium | Native parallel edge centerline detection | `Pattern/RansacLineCore.h`, `Pattern/RansacLineCore.cpp`, `Pattern/RansacLineCli.h`, `Pattern/RansacLineCli.cpp` | — | Added BeeCpp RANSAC-based longest parallel edge-pair detection for arbitrary orientation, returning both edge lines plus the center line between them through the C++/CLI wrapper. Build verified Release x64. |
| V006 | open | medium | Pattern2 false-positive plan | `Pattern/Pattern2.cpp`, `Pattern/Pattern2.h`, `BeeCore/Unit/MultiPattern.cs`, `docs/Pattern2FalseMatchPlan/*` | — | P2-001 debug baseline is done. Continue with `docs/Pattern2FalseMatchPlan/pattern2-map.md` P2-002 to use the new true-positive/false-positive logs and artifacts for shape-validator work. |
| V007 | done | medium | Edge2 parallel-edge preview | `BeeCore/Unit/Edge2.cs`, `BeeInterface/Tool/Tool2Edge.cs` | V005 | Wired Edge2 to BeeCpp `FindLongestParallelPair`, preserved the center line in the existing `Line2DCli` preview path, drew both detected edges plus the center line, and switched Tool2Edge to Edge2 engine helpers. Build verified Release x64. |
| V008 | done | medium | Pin pitch measurement plan | `docs/PinPitchMeasurePlan/*` | — | Planned PinPitch mode for the existing Pitch tool plus PointToLine mode for Width: native pin-center detector, P1..P4 pitch/span, Width distance from selected pin center to reference line L via existing `RansacLine.FindBestLine`, UI modes, and validation path for `+-0.05mm` repeatability. |
| V009 | open | medium | Camera calibration BeeCpp plan | `Pattern/CameraCalibrationCore.h`, `Pattern/CameraCalibrationCore.cpp`, `Pattern/CameraCalibrationCli.h`, `Pattern/CameraCalibrationCli.cpp`, `Pattern/Pattern.vcxproj`, `docs/CameraCalibrationPlan/*` | — | Plan native BeeCpp calibration for chessboard/circle grids, preview undistort/rectify, automatic known-sample scale detection, and simple live pose guidance. Detailed tracker: `docs/CameraCalibrationPlan/camera-calibration-map.md` CC-001/CC-002/CC-004/CC-006. |
