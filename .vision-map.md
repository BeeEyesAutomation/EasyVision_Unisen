# Vision Map

## ID Rules

- Prefix: `V`
- Format: `Vxxx`
- Next ID: `V005`
- Reuse existing IDs.
- Do not duplicate IDs.

## Status values: `open` | `wip` | `partial` | `done` | `blocked`
## Risk values: `low` | `medium` | `high`

## Entries

| ID | Status | Risk | Area | Files | Depends on | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| V001 | open | medium | YOLO result filtering behavior | `BeeCore/Unit/Yolo.cs` | — | Changed to reject detections outside crop scan via `IsRectInsideCropScan`, keep only highest-score filtered item with `FirstOrDefault`, and change mask/scan handling around `Dir.Mask`. Confirmed vision behavior change — validate with representative images containing multiple detections, edge-of-crop detections, and masked regions. |
| V002 | open | medium | Measurement scoring behavior | `BeeCore/Unit/ColorArea.cs`, `BeeCore/Unit/Width.cs` | V004 | Changed `ColorArea.cs` thresholds/ROI offset handling and `Width.cs` score calculation from percentage deviation to absolute rounded difference. Affects inspection results — validate with representative measurement images against known-good expected values. |
| V003 | open | medium | Filter/noise behavior | `BeeCore/Algorithm/Filters.cs` | — | `ClearNoise` calls changed from scalar overloads to `Scalar.All(...)` and morphology/filter formatting updated nearby. Overload fix may be correct but image-processing output should be regression-checked because these methods feed multiple vision tools. |
| V004 | open | low | Width edge mode regression | `BeeCore/Unit/Width.cs` | — | Confirmed pipeline regression: the edge-processing switch no longer handles `MethordEdge.InvertBinary`, so tools saved with that mode skip assigning `matProcess` before noise/morphology and measurement steps. Fix: restore the missing `InvertBinary` case in the switch statement. Blocks: V002 Width score validation. |
