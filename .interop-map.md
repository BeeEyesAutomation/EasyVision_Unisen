# Interop Map

## ID Rules

- Prefix: `I`
- Format: `Ixxx`
- Next ID: `I005`
- Reuse existing IDs.
- Do not duplicate IDs.

## Status values: `open` | `wip` | `partial` | `done` | `blocked`
## Risk values: `low` | `medium` | `high`

## Entries

| ID | Status | Risk | Area | Files | Depends on | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| I001 | done | medium | Native debug link settings | `Pattern/Pattern.vcxproj` | — | Documented that Debug x64 intentionally uses release-compatible iterator/runtime settings and release OpenCV/ZXing libraries to preserve the C++/CLI ABI expected by C# callers and the available native third-party binaries. |
| I002 | done | low | Native version prebuild script | `BeeCV/BeeCV.vcxproj`, `OKNG/OKNG.vcxproj`, `tools/update_native_version.ps1` | — | Prebuild events now call `powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass` with the checked-in `tools/update_native_version.ps1`; Release x64 build verifies the script path executes from MSBuild. |
| I003 | done | â€” | Unmanaged buffer ownership | `BeeCore/Unit/MultiPattern.cs` | â€” | Resolution: `Pattern2.SetImgeSample(...)` `AllocHGlobal` buffer is now freed in a `finally` block after cloning the returned buffer into a bitmap. Verify with repeated MultiPattern calibration/sample runs and native memory monitoring. |
| I004 | open | medium | Camera calibration C# interop contract | `Pattern/CameraCalibrationCli.h`, `Pattern/CameraCalibrationCli.cpp`, `BeeGlobal/Global.cs`, new `BeeGlobal/*CameraCalibration*.cs`, `BeeCore/Data/Access.cs`, `BeeCore/Data/LoadData.cs`, `BeeCore/Data/SaveData.cs`, `BeeCore/Func/*`, `docs/CameraCalibrationPlan/*` | V009 | Define the managed contract for calibration solve, preview undistort/rectify, automatic known-sample scale detection, simple guidance data, and separate `Global.CameraCalibration` persistence without changing unrelated native wrapper APIs or unmanaged ownership rules. |
