# Interop Map

## ID Rules

- Prefix: `I`
- Format: `Ixxx`
- Next ID: `I004`
- Reuse existing IDs.
- Do not duplicate IDs.

## Status values: `open` | `wip` | `partial` | `done` | `blocked`
## Risk values: `low` | `medium` | `high`

## Entries

| ID | Status | Risk | Area | Files | Depends on | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| I001 | open | medium | Native debug link settings | `Pattern/Pattern.vcxproj` | — | Debug\|x64 now compiles with `_ITERATOR_DEBUG_LEVEL=0`, `RuntimeLibrary=MultiThreadedDLL`, and links `opencv_world455.lib`/`ZXing.lib` instead of the debug OpenCV library. May be intentional for ABI compatibility, but changes the native/debug boundary. Verify against C# caller expectations before shipping a debug build. |
| I002 | open | low | Native version prebuild script | `BeeCV/BeeCV.vcxproj`, `OKNG/OKNG.vcxproj`, `tools/update_native_version.ps1` | — | Inline native version update commands replaced by `tools/update_native_version.ps1`. Wrapper API unchanged, but native builds now depend on that script path and PowerShell execution from MSBuild. Verify script is present and executable on CI and clean checkouts. |
| I003 | done | — | Unmanaged buffer ownership | `BeeCore/Unit/MultiPattern.cs` | — | Resolution: `Pattern2.SetImgeSample(...)` `AllocHGlobal` buffer is now freed in a `finally` block after cloning the returned buffer into a bitmap. Verify with repeated MultiPattern calibration/sample runs and native memory monitoring. |
