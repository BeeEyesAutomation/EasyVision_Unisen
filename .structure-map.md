# Structure Map

## ID Rules

- Prefix: `S`
- Format: `Sxxx`
- Next ID: `S015`
- Reuse existing IDs.
- Do not duplicate IDs.

## Status values: `open` | `wip` | `partial` | `done` | `blocked`
## Risk values: `low` | `medium` | `high`

## Entries

| ID | Status | Risk | Area | Files | Notes |
| --- | --- | --- | --- | --- | --- |
| S001 | done | medium | Repository structure | `EasyVision.sln`, all `.csproj`/`.vcxproj` | Added ownership-boundary comments to solution project files documenting the C# app/UI/core/global/test projects and native/C++ wrapper projects in-place, so the repository structure explains the UI, vision core, camera SDK, PLC, updater, test, and wrapper ownership split. Build verified Release x64. |
| S002 | done | medium | Form event handlers | `BeeInterface/Tool/*.cs`, `BeeInterface/GeneralSetting.cs`, `BeeInterface/PLC/ProtocolPLC.cs`, `BeeInterface/Group/View.cs` | Added class-level ownership comments to dense handler surfaces (`ToolYolo`, `ToolPattern`, `GeneralSetting`, `ProtocolPLC`) clarifying that forms own UI binding/orchestration while processing, persistence, and transport behavior stay in core/protocol layers. Build verified Release x64. |
| S003 | done | medium | UI and processing mix | `BeeInterface/Tool/`, `BeeCore/Unit/` | Added ownership notes in `BeeInterface/Tool/OWNERSHIP.md` and `BeeCore/Unit/OWNERSHIP.md` documenting that WinForms tool forms own UI binding/orchestration while `BeeCore.Unit` owns parameters, processing, scoring, and results. |
| S004 | partial | medium | Camera and hardware SDK placement | `BeeCore/Func/Camera.cs`, `BeeInterface/Steps/SettingStep1.cs`, `BeeInterface/Group/View.cs`, `BeeUi/Form/ScanCCD.cs`, `BeeCV/CCD.cpp`, `PylonCli/CameraWrapper.cpp` | Audit 2026-05-11: entire `BeeCore/Camera/` folder (17 files including `HEROJE.cs`, `USB.cs`, `HardwareEnum.cs`, `KEY_Send.cs`, CB structs, ToolCfg, etc.) has been deleted from working tree — managed camera ownership now consolidates under `BeeCore/Func/Camera.cs` + `CameraIOFast.cs` and native wrappers (`BeeCV/CCD.cpp`, `PylonCli`). Ownership comments on the now-deleted Camera/* files no longer apply; refresh remaining comments in `BeeCore/Func/Camera.cs`. Build not verified by audit. |
| S005 | done | low | Wrapper boundary | `BeeCV/`, `PylonCli/`, `Pattern/`, `BeeNativeOnnx/`, `BeeNativeRCNN/`, `ColorPixelsCPP/`, `OKNG/` | Added `BeeCV/INTEROP_BOUNDARY.md` to define the C# callable wrapper surfaces versus native implementation details for the scoped wrapper projects. |
| S006 | done | low | PLC and industrial IO | `PLC_Communication/`, `BeeInterface/PLC/`, `BeeGlobal/Protocol/` | Added `PLC_Communication/OWNERSHIP.md` to document ownership boundaries between low-level transport code, runtime protocol state, and WinForms PLC configuration UI. |
| S007 | done | low | Duplicated UI layers | `BeeInterface/Group/View.cs`, `BeeUi/Form/ScanCCD.cs` | Added class-level ownership comments clarifying that `BeeInterface.View` owns the embedded runtime inspection/live view while `BeeUi.ScanCCD` owns standalone camera discovery/connection UI. |
| S008 | partial | low | Large files | `BeeInterface/GeneralSetting.Designer.cs`, `BeeInterface/Tool/*.Designer.cs`, `BeeInterface/Group/View.cs`, `BeeCore/Unit/Yolo.cs` | Audit 2026-05-11: `BeeCore/Camera/HEROJE.cs` was deleted with the rest of `BeeCore/Camera/` (see S004). Remaining large hand-written files: `BeeCore/Unit/Yolo.cs` (still has guide comments) and several `BeeInterface/Tool/*.Designer.cs` generated files. |
| S011 | partial | medium | Duplicate geometry helpers — FilletCornerMeasure shared methods | `BeeCore/Algorithm/FilletCornerMeasure.cs`, `BeeCore/Algorithm/FilletCornerMeasure2.cs`, `BeeCore/Algorithm/Gap.cs`, `BeeCore/Algorithm/DetectIntersect.cs` | Progress: extracted `DistancePointLine`, `NearestIndex`, `ArcLengthBetween`, `LineDirectionDeg`, `AngleDiffTo90`, and `GetIntersections` into `BeeCore/Algorithm/ContourGeometry.cs`. `GetIntersections` code compiles past the prior missing-using blocker, but Release x64 verification is blocked by locked output DLLs held by Microsoft Visual Studio 2022 (28704) and EasyVision (16564). Remaining order after a clean verification checkpoint: `NearestIntersectionToPoint`, `ClosestPointToLineNear`, `RansacFitLine`, `ExtractEdgePoints`. |
| S012 | open | medium | Duplicate — CopyToPythonAndDetect in 4 unit files | `BeeCore/Unit/CheckMissing.cs`, `BeeCore/Unit/Patterns.cs`, `BeeCore/Unit/MultiOnnx.cs`, `BeeCore/Unit/MultiPattern.cs` | Near-identical method copied 4 times. Extract to a shared static helper in `BeeCore/Func/PythonDetectHelper.cs`. Verify each caller passes same parameters and handles return value identically before abstracting. Build verify after each replacement. |
| S013 | done | low | Duplicate — CalcRoiRect in MultiOnnx and MultiPattern | `BeeCore/Unit/MultiOnnx.cs`, `BeeCore/Unit/MultiPattern.cs` | Removed the unused duplicate `CalcRoiRect` copy from `MultiOnnx`; `MultiPattern` retains the only active local implementation used by its caller. Build verified Release x64. |
| S014 | partial | low | Duplicate — Converts.cs (2 files) | `BeeCore/Func/Converts.cs`, `BeeCore/Converts.cs` | Audit 2026-05-11: status unchanged. Root `BeeCore/Converts.cs` still exists with `Convert2.NumberFromString` wrapper delegating to `BeeCore.Func.Converts.NumberFromString` (Converts.cs:6-10). Last caller: `BeeCore/Unit/Yolo.cs:1563` (`Convert2.NumberFromString(AddPLCPos)`) — flip this call to `Converts.NumberFromString` then drop the root file + csproj entry. |
| S009 | done | — | Generated and build output in tree | `.gitignore` | Verified: `.gitignore` already excludes `bin`, `obj`, `Release`, `x64`, `.vs`; `git ls-files` shows no tracked output artifacts. No code or project move needed. |
| S010 | done | low | Utility and common code placement | `BeeGlobal/Common/`, `BeeGlobal/General/`, `BeeInterface/Class/`, `BeeInterface/Comunications/`, `BeeUi/Commons/`, `BeeCore/Func/`, `PLC_Communication/HslCommunication.*` | Added `BeeGlobal/Common/OWNERSHIP.md` to document intended ownership for shared UI helpers, BeeInterface utilities, BeeUi controls, BeeCore vision helpers, and PLC transport/vendor helpers. |
